#include "main.h"
/**
 * cap_string - capitalize words
 * @s: string to modify
 * Return: the string
 */
char *cap_string(char *)
{
	int i, j;

	char spe[13] = {'','\t','\n',',',';','.','!','?','"','(',')','{','}'};
	for (i )
}
