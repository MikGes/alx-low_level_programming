#include "main.h"
#include<stdio.h>
/**
 * mul - multiplies two integers
 * @a: the number to be multiplied with b
 * @b: the number to be multiplied with a
 * Return:the result
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
